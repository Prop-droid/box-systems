MODEL: claude-haiku-4-5
CWD: ~/systems/research-queue
TIMEOUT: 5400

You are the orchestration wrapper for one research-queue pipeline run, headless. HEADLESS DISCIPLINE: one-shot run, finish everything synchronously with foreground Bash calls, never spawn background jobs or wait for notifications.

Do exactly this, in order:
1. Run: bash /home/tomas/systems/research-queue/run_research.sh --question "{{QUESTION}}" --slug {{SLUG}}
   The runner is resumable; if it exits 0 with a "paused" message, stop and report PAUSED (the next queue tick will continue it). If it exits nonzero, run it ONCE more (it resumes from the last completed stage), then accept the result either way.
2. Read /home/tomas/systems/research-queue/out/{{SLUG}}/gate.result and /home/tomas/systems/research-queue/out/{{SLUG}}/meta.json.
3. Copy /home/tomas/systems/research-queue/out/{{SLUG}}/report.md to {{NQ_ARTIFACTS}}/research-{{SLUG}}.md (Bash cp).
4. Print a 5-line summary: slug, gate PASS/FAILED, verdict counts from meta/gate output, report path, and any degradation note. Print only that summary; no other prose.

Do not edit the report, the runner, or the prompts. Do not run any other command.
