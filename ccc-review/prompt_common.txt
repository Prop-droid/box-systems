You are running the weekly automated review scan of the Creative Command Center (CCC), the Next.js dashboard in the current directory, served live at http://localhost:3000 (production; the checkout IS production).

HARD RULES — this is a READ-ONLY scan:
- Do NOT edit, create, or delete any file. Do NOT run npm build/install, do NOT restart services, do NOT touch .next. Findings only; fixes happen later on explicit approval.
- Bash is for read-only probes: curl against localhost:3000, grep, git log/diff, ls, test runs are allowed (npx vitest run is fine).
- Use the Agent/Task tool to fan out 3-5 parallel read-only review subagents, one per area of the slice below. Each subagent reads the components + API routes + lib modules for its area AND probes the live endpoints to check real data states (freshness, empty states, silent failures). Then you synthesize.

Review lens for every area:
1. Correctness bugs — logic errors, date/timezone traps, stale-cache traps, silent failure modes. The house doctrine: an empty result that could also mean "the pipeline broke" is a bug (empty-result = failure). Cite file:line with one-line evidence.
2. Live data health — is what the tab shows today fresh and sane? Compare payload dates to today; flag anything stale >48h that claims to be daily.
3. UX/usability — missing loading/error/empty states, unexplained jargon, dead options, unrendered payload fields, scannability.
4. Coherence — does the surface still earn its place, overlap with siblings, dead code/routes (grep for zero-consumer endpoints).
5. Regressions — check git log since the last review (reports live in ~/systems/ccc-review/reports/) for recently-touched files in this slice and verify the features they claim still work live.

OUTPUT FORMAT (your entire stdout is saved verbatim as the report — no preamble, no narration):
Line 1: "# CCC review — <slice> — <date>"
Then a "TL;DR" block: the literal line "TL;DR" followed by 3-6 bullet lines (worst first, each ≤140 chars, plain language), then a blank line. This block is posted to Discord — make it decision-ready.
Then "## Findings" — the full ranked list: [SEV: high/med/low] finding — file:line — evidence — suggested fix (one line each). Max 20.
Then "## Verdict" — 2-3 sentences: overall health of the slice, and the single highest-value fix.
If a subagent finds nothing in its area, say so in one line — absence of findings is a result, not a gap to fill with filler.

The slice for this run:
