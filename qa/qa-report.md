# ~/systems QA smoke report

- Run: 2026-07-12 22:25:14 EEST
- Host: tomas-agent-box
- Checks: 10 pass, 0 fail, 16 gaps (no safe dry-run)

## PASS

- research-lanes — research-agent lanes unit tests
- compliance-eval — compliance-eval scorer test
- creative-feedback — creative-feedback gate/promote tests
- usage-guard — usage-guard run + window JSON parses
- fable-resume — fable-resume no-op / launch
- md-server — md-server serving :8092
- bq-clickup-perf — bq-clickup-perf PERF_DRY_RUN (writes nothing)
- launch-autofill — launch-autofill convention lint (reads only)
- iteration-sugg — iteration-suggestions DRY_RUN (no post)
- research-deepdive — research-agent deepdive --dry-run

## Gaps — no safe dry-run mode (not run)

- **winners-refresh** — no dry-run; writes to ClickUp (idempotent but real)
- **comments-digest** — no flag; needs BQ + writes digest md (exits 0 on no data by design)
- **atria-weekly** — no dry-run; live Atria swipe pulls
- **research-agent monitor.sh** — no dry-run; writes real competitor diff into /research feed
- **sha-weekly-report** — no dry-run; burns Claude window tokens
- **agents (weekly/monthly)** — no dry-run; each agent burns Claude window tokens
- **gbrain-weekly** — no dry-run; spends up to $5 real embedding/doctor budget
- **fatigue-sentinel** — --dry-run exists but sends a real [TEST] ntfy push (side effect)
- **box-doctor** — no dry-run; auto-fixes symlinks + reruns guard + sends ntfy (is itself a diagnostic)
- **watchdog** — no dry-run; sends ntfy summary on every run
- **transcript-janitor** — no dry-run; gzips/archives transcript files
- **night-queue** — driver executes real queued headless jobs
- **fable-window** — driver executes real headless claude jobs
- **task-lessons synth** — no dry-run; recall.sh is safe but not a schedulable job
- **headless claude probe** — consumes Claude window tokens; run manually via VERIFY block, not weekly
- **litellm** — dormant proxy; nothing points at it (delete candidate)

_Generated by qa/smoke.sh. Silent when green; ntfy digest sent only on FAIL._
