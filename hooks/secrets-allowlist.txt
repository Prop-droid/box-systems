# Shared secret-scan allowlist for the estate pre-commit hook.
# One ERE (extended regex) per line. A staged added line that matches ANY
# pattern here is exempt from the secret scan. Blank lines and #comments ignored.
# For a one-off exemption prefer an inline  # pragma: allowlist secret  on the
# line itself, or a per-repo  <repo>/.secrets-allowlist  file.
#
# Keep this list tight — every entry is a hole in the scan.
#
# (no shared allowlist entries yet)
