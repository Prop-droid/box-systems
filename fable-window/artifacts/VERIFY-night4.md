# VERIFY-night4 (2026-07-05)

Independent verification pass over night-4 live-exception tasks (RULES amendment, rule 9/10). Method: grep spot-checks against source docs, live system inspection (ss, systemctl, curl), and mechanical frontmatter/placeholder/dash checks. No files modified except this report and the ledger append.

## Pass/fail table

| # | Check | Result | Evidence |
|---|---|---|---|
| 1 | Wiki fixes (task 30) actually applied in `~/brain` wiki files, matching `wiki-conflict-sweep.md` / `wiki-fix-apply.SUMMARY.md` | PASS | Spot-checked 5 of 17 findings live: 1.1 (compliance-guardrails.md line 20, appetite-suppressant hard stop present), 1.4 (creative-angles.md line 88, same ban + revocation note), 1.11 (glp1-dosing-companion.md, drug name removed, HCP hedge + compliance note present), 2.1 (brand-fact-rules.md line 29-31, 70-90 range approved, old "no range" rule marked revoked). All matched the summary table exactly. Baseline integrity: `sha256sum -c wiki-fix-baseline.sha256` — 16/16 OK, all 16 files present in both baseline and postedit snapshots. |
| 2a | `roadmap_S27plus.md` — exists, sane, no placeholders, no em/en dashes | PASS | 133 lines, 0 em/en dash chars, 0 TODO/TBD/XXX/FIXME/lorem hits. Sourced citations (BQ30, R15, WK0622, CANON) resolve to real files. |
| 2b | `feedback_fable_window_lessons.md` — exists, memory-shaped, no placeholders, no em/en dashes | PASS | Valid frontmatter (`name`, `description`, `metadata.type: feedback`), 9 numbered lessons each with Why + How to apply + citation, 0 em/en dashes, 0 placeholder hits. |
| 2c | 3 Hermes SKILL.md artifacts — frontmatter, no placeholders, no em dashes | PASS | `hermes-routing-policy.SKILL.md`, `delegate-to-claude.SKILL.md`, `claude-heavy-lifting.SKILL.md` each have valid `name/description/version/author/metadata` frontmatter, 0 em/en dashes. `<...>` tokens found in delegate-to-claude and claude-heavy-lifting are intentional fill-in-the-blank template placeholders (CLARIFICATION_REQUIRED format, task-packet format) — not unfinished-content placeholders; no TODO/TBD/XXX/FIXME/lorem hits in any of the three. |
| 3 | Option B binds (task 34) — `ss -tlnp` shows no LAN binds for the 3 services; tailnet curl 200s | PARTIAL (2/3, matches ledger's own admission) | md-server: `100.107.26.69:8092` only (no `0.0.0.0`), tailnet curl 200. camofox: `127.0.0.1:9377` only, tailnet curl to `:9377/health` refused (000, expected — localhost-only by design), localhost curl 200. CCC: still `*:3000` (all interfaces) — ledger's own task-34 entry documents CCC as a FAILED rebind (Next 14.2.35 ignores `HOSTNAME` env) that was explicitly rolled back to original state, with the real fix flagged as an open question. So the literal claim "no LAN binds for the 3 services" is false for CCC, but this is the documented, intentional outcome, not an undiscovered regression. |
| 4 | Both cron timers listed (task 35) or `atria/BLOCKED.md` exists | PASS, with a gap | `systemctl --user list-timers` shows both `atria-weekly.timer` (next Mon 07:30) and `fatigue-sentinel.timer` (next Mon 08:30), both `enabled`/`active (waiting)`, installed under `~/.config/systemd/user/` and byte-identical to the staged units in `~/systems/{atria-weekly,fatigue-sentinel}/`. **Gap:** no ledger entry exists for task 35 (the enable action itself) — task 14/15 entries cover staging only, not the live enable. RULES rule 4 requires a summary block per task; this one is missing. Not fabricated here since I did not perform the enable and don't have its authorization/timestamp details firsthand. |
| 5 | Box skill descriptions parse (task 36) | PASS | All 15 touched `SKILL.md` files (system-control, clickup-task-creator, shameless-script, fleet-control, dr-script, landing-page-copy, gbrain-tag-audit, email-copy, firecrawl, micro-scripts, script-critique, interrogate, lt-marketplace-search, share-to-phone, firecrawl-interact) have a leading `---`, a closing `---` with no stray blank line before it (the whitespace bug the ledger describes catching), and both `name:`/`description:` fields present. `pyyaml` is not installed on the box so a strict `yaml.safe_load` pass could not be run; the mechanical structural check is the available substitute and is clean on all 15. |
| 6 | Launch-autofill dry-run log exists (task 37) | PASS | `naming-lint-apply.SUMMARY.md` embeds the actual dry-run output (261 tasks in scope, 297 violations/54 fail/243 warn, per-check breakdown) rather than shipping a separate raw log file — treated as the log artifact. Confirmed live: `~/systems/launch-autofill/autofill.py` sha256 `ff6a8cdb...9020` matches `artifacts/autofill.py.replacement` exactly (both hashes identical) and matches the ledger's claimed hash. Grepped the `LINT_ONLY`/`run_lint()` code path for `requests.post/put/patch` or ClickUp create/update calls — zero matches, confirming the dry-run path is genuinely read-only as claimed. |

## Mechanical issues found and fixed

None. Baseline/postedit snapshot counts (16/16), sha256 manifest (16/16 OK), and all 15 skill frontmatter blocks checked out clean on first pass — nothing to fix.

## Open items (not fixed here, flagged for a human)

1. Task 35 (enabling the two systemd timers) has no `_ledger.md` entry — the live state is correct and verified, but the paper trail RULES rule 4 requires is missing. Recommend whoever ran it back-fill a ledger entry with the actual authorization/timestamp, rather than this pass fabricating one.
2. CCC (port 3000) remains LAN-exposed; this is already flagged as an open question in `security-optionB.APPLIED.md` (task 34) with a concrete fix (`next dev -H 127.0.0.1`) — re-flagged here only because it's the one place this verify pass's "3 services" checklist doesn't fully close.
3. 4 of the 15 skill-description backups (`dr-script`, `landing-page-copy`, `email-copy`, `micro-scripts`) are not true pristine originals per task 36's own incident writeup — live files are confirmed correct, only the rollback point for those 4 is imperfect. Carried forward from task 36, not a new finding.
